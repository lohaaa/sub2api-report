{
  "name": "sub2api-report",
  "private": true,
  "version": "1.1.1",
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "dev:web": "pnpm --dir web run dev",
    "build:web": "pnpm --dir web run build",
    "lint:web": "pnpm --dir web run lint",
    "test:web": "pnpm --dir web run test",
    "typecheck:web": "pnpm --dir web run typecheck",
    "quality:dotnet": "dotnet format Sub2ApiReport.slnx --verify-no-changes --no-restore && dotnet build Sub2ApiReport.slnx --configuration Release --no-restore && dotnet test Sub2ApiReport.slnx --configuration Release --no-build",
    "quality:web": "pnpm run typecheck:web && pnpm run lint:web && pnpm run test:web && pnpm run build:web",
    "quality": "pnpm run quality:dotnet && pnpm run quality:web"
  }
}
